
# FreeCAD Toolkit

This repo hold the shared code between plume and frameforge.

It should be used as a a git subtree, or as a standalone freecad package (I don't like this solution because I can't find a good name for it...)
library ? toolkit ? too vague...